spring:
  profiles:
    active: native
---
spring:
  config:
    activate:
      on-profile: native
  cloud:
    config:
      server:
        native:
          search-locations: file://#{CONFIG_REPO_ABS_PARENT}
